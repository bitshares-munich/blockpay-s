package de.bitsharesmunich.utils;

/**
 * Enum class used to specify the type of transfer we'll be dealing with
 */
public enum TransferType {
    PLATFORM_ASSET,
    BRIDGE_DIRECT,
    BRIDGE_INDIRECT
}
